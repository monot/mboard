<%= simple_form_for([@message, @message.comments.build]) do |f| %>
  <%= f.input :content, placeholder: "댓글을 입력해주세요" %>
  <div class="write-btn">
    <%= f.button :submit, value: '확인', class: 'round-btn comment-btn' %>
  </div>
<% end %>
