<!DOCTYPE html>
<!--global head-->
<html>
  <head>
    <title>Message to Prof. Kang!</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if params[:action] == 'index' %>
    <div id="header" class="header">
      <div class="container">
        <div class="row">
          <div class="col-xs-12 col-md-12 nav-glb">
            <div><%= link_to "홈", root_path %></div>
            <div><%= link_to "프레젠테이션 모드", messages_presentation_path %></div>
            <% if user_signed_in? %>
            <div><%= link_to "로그아웃", destroy_user_session_path, method: :delete  %></div>
            <% else %>
            <div><%= link_to "가입", new_user_registration_path %></div>
            <div><%= link_to "로그인", new_user_session_path %></div>
            <% end %>
          </div>
        </div>

        <div class="container">
          <div class="row">
            <div class="col-xs-12 col-md-12 site-title">
              강남준 선생님께<br />
              보내는 메시지
            </div>
          </div>
        </div>

        <% if user_signed_in? %>
        <div class="container">
          <div class="row">
            <div class="col-xs-12 col-md-12 msg-compose">
              <%= link_to "새 메시지 작성", new_message_path, class: "round-btn title-btn"%>
            </div>
          </div>
        </div>
        <% end %>

        <p>

        </p>
      </div>
    </div>
    <% elsif params[:action] == 'presentation' %>
          
    <% else %>
    <div id="header" class="header-narrow">
      <div class="container">
        <div class="row">
          <div class="col-xs-12 col-md-6 narrow-title">
            강남준 선생님께 보내는 메시지
          </div>
          <div class="col-xs-12 col-md-6 nav-glb">
            <% if user_signed_in? %>
              <div><%= link_to "새 메시지 작성", new_message_path, class: "round-btn title-btn" %></div>
            <% end %>
            <hr />
            <div><%= link_to "홈", root_path %></div>
            <div><%= link_to "프레젠테이션 모드", messages_presentation_path %></div>
            <% if user_signed_in? %>
            <div><%= link_to "로그아웃", destroy_user_session_path, method: :delete  %></div>
            <% else %>
            <div><%= link_to "가입", new_user_registration_path %></div>
            <div><%= link_to "로그인", new_user_session_path %></div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <% end %>

    <div class="toastArea">
      <% if notice != nil and notice != "" %>
        <div class="toast t-notice">
          <%= notice %>
        </div>
      <% end %>

      <% if alert != nil and alert != "" %>
        <div class="toast t-alert">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <script>
      $(".toast").each(function(i) {
        $(this).delay(500*i).slideDown(500);
        $(this).delay(500*i + 1500).slideUp(500);
      });
    </script>

    <div class="container">
      <%= yield %>
    </div>

    <footer class="bd-footer text-muted text-center">
      <div class="container">
        <hr />
        강남준 선생님께 보내는 메시지&nbsp;&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;&nbsp;서울대학교 언론정보학과
        <br />
        <br />
      </div>
    </footer>

  </body>
</html>
